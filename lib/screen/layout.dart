import 'dart:html';

import 'package:flutter/material.dart';

class LayoutSceen extends StatelessWidget {
  const LayoutSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Card(),
    );
  }
}
